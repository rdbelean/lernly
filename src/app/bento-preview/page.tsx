// TEMPORARY visual-QA route for FeatureBento — delete before merge.
import FeatureBento from "@/components/landing/FeatureBento";

export default function BentoPreviewPage() {
  return (
    <main style={{ backgroundColor: "#0F1322", minHeight: "100vh" }}>
      <FeatureBento />
    </main>
  );
}
